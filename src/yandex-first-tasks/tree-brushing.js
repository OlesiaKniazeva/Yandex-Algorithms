
function getInput() {
  /* fix */
}

function sendOutput() {
  /* */
}

/**
 * 
 * @param {[number, number]} First person tree index and distance 
 * @param {[number, number]} Second person tree index and distance  
 * @returns number
 */
var countTreesAmount = function ([index1, distance1] = [], [index2, distance2] = []) {
  let treeAmount = 0;

  return treeAmount;
};

exports.module = { countTreesAmount };
